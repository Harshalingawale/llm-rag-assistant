# Retrieval-Augmented Generation (RAG)

RAG combines a retriever and a generator. The retriever finds relevant text
chunks from a knowledge base using vector similarity search; the generator (an
LLM) conditions its answer on those chunks. This grounds answers in source
documents and reduces hallucination.
