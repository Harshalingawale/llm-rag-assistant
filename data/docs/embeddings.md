# Sentence Embeddings

An embedding maps text to a dense vector so that semantically similar texts are
close in vector space. all-MiniLM-L6-v2 produces 384-dimensional embeddings and
is a fast, strong default for retrieval. Cosine similarity ranks chunks by
relevance to a query.
